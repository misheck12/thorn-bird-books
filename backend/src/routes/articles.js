const express = require('express');
const router = express.Router();
// Placeholder routes

router.get('/', (req, res) => {
  res.json({ message: 'Articles endpoint - To be implemented' });
});

module.exports = router;