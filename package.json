# Thorn Bird Books - Root Package Manager

This is the root directory for the Thorn Bird Books platform. The project consists of multiple applications:

## Applications

- **backend/**: Node.js API server
- **frontend/**: User-facing React application  
- **admin/**: Admin dashboard React application

## Quick Start Commands

### Install all dependencies
```bash
# Install backend dependencies
cd backend && npm install

# Install frontend dependencies  
cd frontend && npm install

# Install admin dependencies
cd admin && npm install
```

### Start all applications
```bash
# Start backend (in one terminal)
cd backend && npm run dev

# Start frontend (in another terminal) 
cd frontend && npm start

# Start admin (in another terminal)
cd admin && npm start
```

## Development URLs

- **Frontend**: http://localhost:3000
- **Admin**: http://localhost:3001  
- **Backend**: http://localhost:5000

## Documentation

See the main README.md for comprehensive setup and development instructions.